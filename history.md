# 開発履歴 (history.md)

## 2026-07-31
### 実施内容: Splitフレーム欠落と蓄積値による暴走対策
- **目的**: Split BLEでX/Yの一方が欠落した場合や、レポート間隔中の蓄積値が過大になった場合でも、後続フレームでカーソル・スクロールが暴走しないようにする。
- **ブランチ**: `Dev-v0.3_inertial-scroll`

#### 技術的詳細
1. **Split Peripheralの軸イベントを独立sync化**:
   - PeripheralではX/Yをそれぞれ完結したsyncイベントとして送信する。
   - 一方のBLE通知が欠落しても、未syncの値が後続フレームへ合算されない。
   - Centralおよび非Split構成では従来どおりX/Yを1フレームにまとめる。
2. **合算レポートの上限追加**:
   - `max-report-delta`を追加し、複数サンプルを蓄積した後の1レポートを既定±2047に制限する。
   - 超過分は後から連続放出せず破棄し、遅延ジャンプや長い慣性生成を防ぐ。

#### 影響範囲
- `src/pmw3610.c`, `src/pixart.h`, `dts/bindings/pixart,pmw3610.yml`
- PMW3610を搭載するCentral / Peripheralの両方。
- レイヤー、CPI、XY変換、BLE接続数、スタック・ACLバッファは変更なし。

## 2026-07-24
### 実施内容: 縦スクロール方向トグルBehavior
- **目的**: キー操作で縦スクロールの正転・逆転を切り替えられるようにする。
- **ブランチ**: `Dev-v0.3_inertial-scroll`

#### 技術的詳細
1. **グローバルBehaviorの追加**:
   - `&pmw3610_scroll_direction_toggle` を追加。
   - `BEHAVIOR_LOCALITY_GLOBAL` により、CentralとPeripheralの両方で状態を切り替える。
2. **センサー単位の方向状態**:
   - PMW3610インスタンスごとに縦方向反転状態を保持。
   - `inertial-scroll-layers` が指定されている場合は対象レイヤーだけに適用し、通常カーソル方向には影響しない。
3. **慣性との整合性**:
   - 切替時に進行中の慣性を停止し、切替前方向のスクロールが残らないようにする。
   - 物理移動を反転してから慣性速度を生成するため、通常スクロールと慣性スクロールの方向を一致させる。

#### 影響範囲
- `src/pmw3610.c`, `src/pmw3610.h`, `src/pixart.h`
- `src/behavior_pmw3610_scroll_direction_toggle.c`
- `dts/behaviors/pmw3610_scroll_direction_toggle.dtsi`
- `dts/bindings/behaviors/zmk,behavior-pmw3610-scroll-direction-toggle.yaml`
- PMW3610を搭載するCentral / Peripheralの両方。
- BLE、CPI、XY変換、既存の慣性パラメータは変更なし。

## 2026-07-23
### 実施内容: PMW3610 SPI読み取りのtSRAD保証
- **目的**: 左右トラックボールで発生するカーソル暴走に対し、PMW3610が要求するアドレス送信後の読み取り待機時間を保証する。
- **ブランチ**: `Dev-v0.3_inertial-scroll`

#### 技術的詳細
1. **CS保持中の二段階読み取り**:
   - `src/pmw3610.c`: `SPI_HOLD_ON_CS | SPI_LOCK_ON` を指定し、アドレス送信とデータ受信を別トランザクションに分割。
   - 両トランザクション間に `k_busy_wait(T_SRAD_DELAY_US)` を追加し、既定の35us待機を保証。
2. **SPIエラー処理の強化**:
   - SPIクロック有効化、アドレス送信、データ受信、ロック解放、クロック無効化の戻り値を確認。
   - 読み取り途中で失敗した場合も `spi_release()` を実行し、CSとSPIロックを確実に解放。

#### 影響範囲
- `src/pmw3610.c` のPMW3610共通SPI読み取り処理。
- PMW3610を搭載するCentral / Peripheralの両方。
- `.conf` / `.overlay` / `.dtsi` / `.keymap` / `west.yml` / GitHub Actionsは未変更。
- レイヤー、board / shield、BLE設定、慣性スクロール設定そのものは変更なし。

## 2026-04-14
### 実施内容: Dev-v0.3_inertial-scroll-v2 への慣性スクロール移植
- **目的**: `dev-v0.3_against-runaway` 系のドリフト対策・FAULT復帰対策を維持したまま、`Dev-v0.3_inertial-scroll` と同等の driver-side 慣性スクロールを `Dev-v0.3_inertial-scroll-v2` に実装する。
- **ブランチ**: `Dev-v0.3_inertial-scroll-v2`

#### 技術的詳細
1. **DTS property の追加**:
   - `dts/bindings/pixart,pmw3610.yml`: `inertial-scroll`、速度/減衰/周期/停止閾値、`inertial-scroll-layers` を追加。
2. **慣性スクロール状態の追加**:
   - `src/pixart.h`: センサーインスタンスごとの delayed work、Q8 velocity/remainder、enable state を追加。
   - `src/pmw3610.h`: Q8 scale と外部操作用 API 宣言を追加。
3. **慣性スクロール処理の移植**:
   - `src/pmw3610.c`: 物理モーション report 後に慣性 velocity を生成し、`k_work_delayable` で減衰した synthetic input report を継続送信する処理を追加。
   - レイヤー制限は `inertial-scroll-layers = <6 7>;` のように数字リストで指定し、未指定時は全レイヤーで許可する。
4. **既存安定化処理との統合**:
   - FAULT 検知時は `dx`/`dy` に加えて慣性状態も破棄し、復帰後のジャンプや不要な synthetic report を防止。
   - 既存の `motion-threshold` ドリフトフィルタ、MOT/FAULT guard、async init retry/backoff、IDLE時 performance 制御は維持。
5. **README更新**:
   - `Dev-v0.3_inertial-scroll-v2` 用の west revision と DTS property 例を追加。
6. **ON/OFF behavior の追加**:
   - `zmk,behavior-pmw3610-inertia-toggle` を追加し、keymap から `&pmw3610_inertia_toggle` のように慣性スクロールをトグルできるようにした。
   - `dts/behaviors/pmw3610_inertia_toggle.dtsi` を追加し、利用側は `#include <behaviors/pmw3610_inertia_toggle.dtsi>` だけで behavior node を取り込めるようにした。

#### 現在の状態メモ
- `Dev-v0.3_inertial-scroll-v2` は remote push 済み。
- 最新 commit は `b4f10f1 docs: add PMW3610 inertia toggle include`。
- `pmw3610_inertia_toggle.dtsi` は driver module 側に存在するが、Keymap Editor 側で `&pmw3610_inertia_toggle` が候補に出ない報告あり。
- 推定原因: ZMK build では west module の `dts/behaviors/*.dtsi` を include 解決できる一方、Keymap Editor が config repo 単体だけを解析して west module 側 include を見ていない可能性がある。
- 次の候補: Keymap Editor 対策として、AAA config repo 側にも `config/behaviors/pmw3610_inertia_toggle.dtsi` 相当を置くか、keymap に node を直接定義する。

#### 影響範囲
- PMW3610 ドライバ内部の input report と delayed work。
- `.overlay` 側で `inertial-scroll` を有効化したセンサーのみ慣性スクロールが動作。
- `.conf` / `.keymap` / `west.yml` / GitHub Actions は未変更。

## 2026-04-13
### 実施内容: PMW3610 停止時ドリフト対策
- **目的**: 操作していない状態でカーソルが勝手に動くドリフト現象を抑制する。
- **ブランチ**: `dev-v0.3_against-runaway`

#### 技術的詳細
1. **微小デルタのフィルタ追加**:
   - `src/pmw3610.c`: `MOTION_BURST` 取得後、X/Y の絶対値が `motion-threshold` 以下の場合はドリフト由来の微小ノイズとして破棄する処理を追加。
2. **センサー単位の調整項目追加**:
   - `dts/bindings/pixart,pmw3610.yml`: `motion-threshold` property を追加。デフォルトは `1`、`0` 指定でフィルタ無効化。
   - `src/pixart.h`: `struct pixart_config` に `motion_threshold` を追加。
3. **設定例の更新**:
   - `README.md`: overlay 例に `motion-threshold = <1>;` を追加。

#### 影響範囲
- PMW3610 ドライバ内部の input report 前フィルタ。
- `.overlay` で `motion-threshold` をセンサーごとに調整可能。
- レイヤー、board/shield、split 設定そのものは変更なし。

## 2026-02-22
### 実施内容: トラックボール暴走およびキー入力スタック問題の対策
- **目的**: しばらく使用した後に発生するトラックボールの制御不能な挙動（暴走）と、それに伴うキー入力の連打・スタック現象の解消。
- **ブランチ**: `dev-v0.3_against-runaway` (base: `zmk-0.3`)

#### 技術的詳細
1. **SPI通信の堅牢化**:
   - `src/pmw3610.c`: アドレス送信から受信の間に `t_SRAD` (35us) のディレイを挿入。データ化けを根本回避。
2. **モーション・データの検証強化**:
   - `src/pmw3610.h`: `MOT`, `FAULT` ビットの定数を追加。
   - `src/pmw3610.c`: `MOTION_BURST` 取得後、`MOT` ビットが立っていない場合はデータを読み飛ばすガードを追加。
   - `src/pmw3610.c`: 12bitの有効範囲を大きく超える不自然なデルタ値（>1024）を異常値として破棄するフィルタを導入。
3. **割り込み制御の最適化**:
   - `src/pmw3610.c`: IRQ設定をレベルトリガーからエッジトリガー（`GPIO_INT_EDGE_TO_ACTIVE`）に変更。不安定時のCPU占有を防止。

#### 影響範囲
- `src/pmw3610.c`, `src/pmw3610.h` のドライバ内部ロジック。
- ZMK v0.3 との互換性を維持。

### 実施内容 (v2: レビュー指摘対応)
- **目的**: 初版実装に対するレビュー指摘（SPI CSデアサート問題、IRQ見逃しリスク、変数競合、FAULTリカバリ）の解決。

#### 技術的詳細
1. **SPI通信の安全確保 (High)**:
   - トランザクション分割によりCSがデアサートされるリスクを重く見て、Zephyr の `SPI_HOLD_ON_CS` に依存しない安全な一括通信（`spi_transceive_dt`）へ回帰しました。
2. **IRQ見逃し防止 (Medium)**:
   - 割り込みをエッジトリガ（`GPIO_INT_EDGE_TO_ACTIVE`）からレベルトリガ（`GPIO_INT_LEVEL_ACTIVE`）へ戻し、一時無効化時のエッジ取りこぼしによる機能停止を防ぎました。
3. **センサーFAULTリカバリ (Medium)**:
   - `MOT` バーストから `FAULT` ビットを検出した際、センサーにソフトリセット（`PMW3610_REG_POWER_UP_RESET`）を送信し、初期化ワークキュー（`init_work`）を再スケジュールする自動復帰ロジックを追加しました。
4. **マルチインスタンス対応 (Medium)**:
   - グローバルな `static` 状態変数（`dx`, `dy`, タイムスタンプ）を `struct pixart_data` のメンバに移行し、2センサー（左右等）構成時でも内部状態が干渉しないようにしました。

### 実施内容 (v3: レビュー指摘対応と既存バグの修正)
- **目的**: v2実装における history.md とコードの乖離の修正、および既存バグの修正。

#### 技術的詳細
1. **IRQトリガ設定の乖離修正**:
   - `history.md` に先行記載されていたレベルトリガ (`GPIO_INT_LEVEL_ACTIVE`) への回帰について、実際のコード (`pmw3610_set_interrupt`) に反映されていなかったミスを修正しました。
2. **FAULTリカバリ時の書き込み関数の統一**:
   - `pmw3610_report_data` 内でのリセット処理において、通常の `pmw3610_write` に統一し、SPIクロック等のハンドリング一貫性を担保しました。
3. **既存バグ `pmw3610_set_performance` の修正**:
   - Performance Register から RUN RATE (4ms/8ms等) を取得・保持する際、下位4bitを残したい意図に対し `0x0F` にマスクされていた既存バグが存在したため、これを正しい `0xF0` に修正しました。

### 実施内容 (v4: 再レビュー指摘対応)
- **目的**: v3実装で生じたコメントの不整合や、レベルトリガ設定による新たな無限ループリスクの解消。

#### 技術的詳細
1. **FAULTリカバリ時の書き込み関数の統一 (反映漏れ修正)**:
   - v3の `history.md` に記載しつつコードに反映しきれていなかった点として、`pmw3610_report_data` 内のリセットコマンドを `pmw3610_write_reg` から `pmw3610_write` へ完全に統一しました。
2. **レベルトリガ＋FAULTリカバリ時のIRQ無限ループ防止 (新規追加)**:
   - レベルトリガ（`GPIO_INT_LEVEL_ACTIVE`）構成のもとでセンサーチップが `FAULT` を発火した場合、リセット後もMOTIONピンがアクティブ状態を維持する可能性があります。その際 `pmw3610_work_callback` の最後で無条件にIRQを再有効化すると即座に割り込みが発生し、完全復旧（約300ms）までビジーポーリングの無限ループに陥るリスクがありました。
   - これを防ぐため、`data->ready` フラグが `false`（初期化中またはリカバリ中）の場合は `pmw3610_work_callback` 内でIRQを再有効化しないガードを追加しました。割り込みの再開は初期化シーケンス完了時の安全なタイミングに委ねられます。
3. **コメントの修正**:
   - `pmw3610_set_performance` において、`perf |= 0xF0;` に対するコメントが `set bit[3..0]` となっていたため、正しい `set bit[7..4]` に訂正しました。

### 実施内容 (v5: 軽微なレビュー指摘対応)
- **目的**: FAULTリカバリ時のリセット処理の安全性の向上（SPI CLK制御の不整合解消とエラーハンドリング）。

#### 技術的詳細
1. **リセットコマンドにおける `pmw3610_write_reg` への復帰**:
   - `pmw3610_write` を用いるとコマンド送信直後に `SPI_CLK_OFF` が一律送信されます。しかし、センサーチップがリセット（再起動）処理に入っている最中に当該信号を送る動作はデータシート上リスキー（状態破壊等）である可能性が指摘されたため、純粋なレジスタアクセスのみを行う `pmw3610_write_reg` に戻し、安全を担保しました。
2. **リセットコマンド送信時の戻り値チェック追加**:
   - `PMW3610_REG_POWER_UP_RESET` へのコマンド書き込み時の戻り値（`err`）を変数で受け、仮にSPIレイヤーでエラーが起きた際には `LOG_ERR` を出力するように改善しました。これにより「無言でリカバリ突入と失敗を繰り返す」事態を防げます。

### 実施内容 (v6: さらなるエッジケースと不要ファイルのクリーンアップ)
- **目的**: FAULTリカバリシーケンスのロバスト化、蓄積データによる挙動不審の防止、およびリポジトリの保全。

#### 技術的詳細
1. **FAULTリカバリ失敗時の停止リスク対策 (High)**:
   - v5まで、`pmw3610_report_data` 内で同期的に送っていたリセット書き込みは、失敗した場合に `init_work` に処理が移っても初期化が中断されるリスクがありました。
   - この同期実行を廃止し、FAULTを検知した時点で `data->async_init_step = ASYNC_INIT_STEP_POWER_UP` とセットして非同期初期化キューへ丸ごと委譲する作戦に変更しました。これにより、標準の初期化フローの中で正しくリセットとその後のリカバリが行われ、仮に失敗しても非同期フローの枠内で適切な状態（`data->ready` フラグ等）が管理されます。
2. **FAULT時の座標・時刻バッファのフラッシュ (Medium)**:
   - FAULT判定フラグが立った時点で、前回までのポーリングで蓄積していた `data->dx`、`data->dy`、およびスリープ関連のタイムスタンプ変数をすべて強制的に `0` へリセットする処理を追加しました。
   - FAULT復帰後に故障前のゴミデータと合算され、カーソルがワープ（ジャンピング）してしまう現象を未然に防ぎます。
3. **コミット成果物のクリーンアップ (Low)**:
   - 誤って git インデックスに含まれていた `build/` ディレクトリ群および `.DS_Store` アセットを `git rm -rf --cached` にて除外しました。

### 実施内容 (v7: gitインデックスの完全整理と再発防止)
- **目的**: 前回作業で漏れていた不要ファイルのコミット除外の完遂と、再発防止の設定。

#### 技術的詳細
1. **`.gitignore` の新設**:
   - リポジトリルートに `.gitignore` を作成し、`build/` と `.DS_Store` を明示的に無視する設定を追加しました。これによりローカルで `west build` 等を行った際にふたたび不要ファイルが追跡対象に上がるのを恒久的に防ぎます。
2. **`git rm -rf --cached` の確実なコミットと誤字修正**:
   - v6での除外状態がコミットツリーに正しく反映されていなかったため、再度 `git rm` を実行して確実な除外コミットを生成しました。また、本ドキュメント内の `FARE` -> `FAULT` の軽微なタイプミスを修正しました。

### 実施内容 (v8: フェイルセーフ拡充とスリープ移行バグの修正)
- **目的**: SPI通信エラー等に伴うセンサー停止リスクの完全排除と、消費電力マネジメントの正常化。

#### 技術的詳細
1. **初期化フェイルセーフ（再試行ロジック）の追加 (High)**:
   - `pmw3610_async_init` 内で実行される各ステップ（例: リセットコマンド送信やレジスタ設定など）が失敗した場合、これまでは `LOG_ERR` を吐いてそのまま停止し、さらに `ready = false` のままとなるため永久にIRQが再開されないデッドロック状態に陥るリスクがありました。
   - これに対し、エラー発生時は `k_work_schedule(&data->init_work, K_MSEC(100))` を呼んで100ms後に同ステップからリトライするロジックを追加し、一時的なSPIエラーやセンサー応答遅延から確実に自己復帰（フェイルセーフ）できるよう強化しました。
2. **`force_awake` 解除時のレジスタマスク修正 (Medium)**:
   - `pmw3610_set_performance` にて `force_awake_4ms_mode == false` かつ `enabled == false` （IDLE移行などの解除要求）の場合、既存コードでは `value & 0xF0` によって上位4bit（force_awake領域）を保持してしまっており、一生省電力モードへ移行できない潜在バグがありました。
   - そこで、ベースの動作レート（下位4bit）をリセットしつつ、awakeフラグ（上位4bit）も確実に落とすため、初期化用マスクを `value & 0x00` に修正（その後 v9 にてさらに `perf = 0x00;` へ可読性改善）し、後続の `enabled` 判定で必要に応じて改めて付与される論理へと適合させました。

### 実施内容 (v9: リトライ上限の設置とマスク可読性の向上)
- **目的**: 永続的なハードウェア障害時の無限リトライ防止、およびコード可読性の改善。

#### 技術的詳細
1. **初期化フェイルセーフのループ制御 (Medium)**:
   - v8で追加したリトライ機能について、障害が永続する場合に無限リトライとログ出力を繰り返すリスクを排除するため、`struct pixart_data` に `uint8_t init_retries` カウンタを追加しました。
   - 各ステップでのエラー時に最大3回までリトライし、それでも失敗した場合は諦め（停止）、それ以降のリクエストを行わない設計としました。これによりバッテリー枯渇やログスパムを防ぎます。
   - FAULTからの復帰時など、初期化シーケンスの開始時には必ず `data->init_retries = 0` へリセットされるよう実装しています。
2. **`force_awake` 解除マスクの可読性向上 (Low)**:
   - `pmw3610_set_performance` において、`value & 0x00` という論理的に冗長（必ず0）な演算について、不要な参照による混乱を防ぐため `perf = 0x00;` というシンプルな代入に書き換えました。
   - 合わせて、レジスタから読み出した `value` 変数は後続の書き込みスキップ最適化（`if (perf != value)`）のためだけに利用されている旨を明記するコメントを追加し、コードの意図を明確にしました。

### 実施内容 (v10: 長期フェイルセーフの確立とIDLE遷移の適正化)
- **目的**: 設定の論理不整合解消（IDLE時に省電力になる本来の動作への修正）と、初期化失敗時の極端な文鎮化（永久停止）の回避。

#### 技術的詳細
1. **`force_awake_4ms_mode` 時の IDLE 移行ロジック修正 (Medium)**:
   - `pmw3610_set_performance` にて、`force_awake_4ms_mode = true` の場合、`enabled = false` （IDLE遷移時）であっても常に下位ビットが 4ms（`0x0d`）のまま維持されるバグがありました。
   - 修正として、必ずデフォルトで `perf = 0x00;` （8ms, IDLE状態）に落としたうえで、`enabled = true` のときのみ `0x0d` への変化や `force_awake` ビット（`0xF0`）の付与を行う設計に見直しました。これにより、README で謳われている通り「ACTIVE移行時のみ4ms化し、IDLE時にはしっかり省電力」という挙動に戻りました。
2. **初期化エラー上限到達時のバックオフ追加（文鎮化回避） (Medium)**:
   - v9の「3回リトライで諦める」という対応では、バッテリー消費・ログスパムこそ止まりますが、突発ノイズや数秒間にわたる接触不良などの後に デバイスが `ready = false` のまま永久に沈黙（文鎮化）し、リブートするまで復帰できなくなる問題が残っていました。
   - これに対し、「3回リトライ失敗後は完全に諦める」のではなく、「10秒間という長いバックオフ（待機）を挟んだうえで初期化フローを最初からやり直す（`data->async_init_step = ASYNC_INIT_STEP_POWER_UP;`）」という遅延再実行スケジュールを導入しました。これにより「ログスパムと過負荷」は防ぎつつ、「10秒毎に生存確認をして、いつか復帰できる永続フェイルセーフ」を実現しています。


### 実施内容 (v11: 残存していた非対称性と競合エッジケースの解消)
- **目的**: SPI通信におけるクロック操作の非対称性解消と、アイドル移行・復帰時の初期化シーケンス競合の防止による「アイドル後の一瞬の暴走」対策の完遂。

#### 技術的詳細
1. **SPIクロック制御の対称性確保 (`pmw3610_read`) (High)**:
   - `pmw3610_read` 内で行われる一括SPIトランザクション (`spi_transceive_dt`) において、これまではSPIクロックの有効化（`PMW3610_REG_SPI_CLK_ON_REQ` の送信）および規定ディレイが省かれており、書き込み（`pmw3610_write`）に対して非対称な状態でした。
   - REST3など深いスリープモードからの読み取り直後にSPIクロックの立ち上がりが間に合わずゴミデータ（0xFFやMOT=1/FAULT=0の中途半端な値）を引く恐れが残っていたため、`pmw3610_read` にも書き込み時と同様にクロック有効化→ディレイ→読み取り→無効化のシーケンスを追加し、通信の堅牢性をさらに高めました。
2. **パフォーマンス状態変更時の初期化シーケンス競合回避 (Medium)**:
   - アクティビティ状態変更フック (`on_activity_state`) から非同期で呼ばれる `pmw3610_set_performance` において、万が一センサーがFAULTリカバリ等により初期化シーケンス中（`data->ready == false`）である場合に呼ばれた際、レジスタ操作が競合して初期化状態を破壊するリスクがありました。
### 実施内容 (v12: バージョン11で混入した初期化ブロッカーバグの修正)
- **目的**: v11の `pmw3610_set_performance` へのガード追加によって生じた「初期化シーケンスの進行不能（文鎮化）」リグレッションの解消。

#### 技術的詳細
1. **`data->ready` チェック位置の適正化 (High)**:
   - v11にて、外部からの不用意な設定変更を防ぐ目的で `pmw3610_set_performance` の先頭に `if (unlikely(!data->ready)) { return -EBUSY; }` を追加しました。しかし、この関数はセンサー初期化フロー（`pmw3610_async_init_configure`）の一部としても `data->ready == false` の状態で呼び出されるため、初期化自体も `-EBUSY` で弾かれ、リトライとバックオフの無限ループに陥る重篤なバグを埋め込んでいました。
   - 修正として、`pmw3610_set_performance` 内部のガードを削除し、代わりに外部からの割り込み要求元である `on_activity_state` フック側に `if (likely(data->ready))` の条件判定を移動しました。これにより初期化・リカバリ等の内部フローは正常に進行しつつ、状態変更イベント経由の競合のみを安全にブロックできるよう改善しました。

### 実施内容 (v13: C11暗黙的関数宣言の防止)
- **目的**: v11でのSPIクロック制御追加に伴い発生した `pmw3610_write_reg` 関数の前方参照（Implicit function declaration）エラー／警告の解消。

#### 技術的詳細
1. **関数プロトタイプ宣言の追加 (Medium)**:
   - v11にて `pmw3610_read` 関数内に `pmw3610_write_reg` の呼び出しを追加した結果、元のファイル構成では定義順序が逆転して前方参照状態となっていました。C11規格および Zephyr の標準的なビルド設定（`-Werror=implicit-function-declaration`等）の下ではコンパイルエラーとなるリスクがありました。
   - `pmw3610_read` の直前（`//////// Function definitions //////////` ブロックの直下）に、`pmw3610_read_reg` および `pmw3610_write_reg` の静的関数プロトタイプ宣言（`static int ...;`）を明示的に追加しました。これにより安全かつ規格に準拠した形で関数呼び出しが行われるよう修正しました。

### 実施内容 (v14: タッチパッド型のフリック速度推定)

- **目的**: 最後の1サンプルだけで慣性初速が決まり、速く回しても指を離す直前の減速によって慣性が弱くなる問題を改善。
- **変更内容**:
  - 直近の入力からジェスチャー速度を平滑化して保持。
  - 加速時は新しい速度へ素早く追従し、減速時は速度を緩やかに追従させる非対称フィルターを採用。
  - 80msを超える入力間隔とスクロール方向の反転を、新しいジェスチャーとして扱う。
  - 慣性ゲインと既存の減衰・停止処理は、平滑化した速度に対して適用。
- **影響範囲**:
  - `inertial-scroll` を有効にしたPMW3610のみ。
  - 通常ポインター、SPI、CPI、BLEおよびメモリー設定は変更なし。

### 実施内容 (v15: センサー取り付け方向に対応したスクロール反転)

- **目的**: 90度回転して搭載したセンサーで、縦スクロール方向トグルが生のY軸を反転しても最終的な縦スクロールへ反映されない問題を修正。
- **変更内容**:
  - センサー単位の `vertical-scroll-uses-x-axis` Devicetreeプロパティを追加。
  - 指定時はY軸ではなくX軸を縦スクロール方向トグルの対象にする。
- **影響範囲**:
  - 新プロパティを指定したセンサーのみ。未指定時は従来どおりY軸を反転。
  - 慣性速度推定、CPI、SPI、BLE設定は変更なし。

### 実施内容 (v16: v2安全性監査と縦横スクロール制御)

- **目的**: 暴走・フリーズ経路を再監査し、片側センサー構成と横スクロール方向切替へ対応。
- **安全性修正**:
  - 3回連続するモーション読出しエラーを検知して非同期初期化へ移行。
  - FAULTをMOTビットより先に検査し、FAULT時は移動量・慣性・速度履歴を一括破棄。
  - レポート後のIRQ再有効化失敗と電源状態変更失敗を検知して再初期化。
  - 複数SPI転送からなる読書きシーケンスをセンサー単位のmutexで直列化。
  - 書込み失敗時もSPIクロック無効化を試行するよう全終了経路を整理。
  - 慣性減衰の乗算を64bit化し、Devicetree設定値をビルド時に検証。
- **機能追加**:
  - 横スクロール方向トグルGlobal Behaviorを追加。
  - 縦横の方向反転を通常スクロールと慣性スクロールへ共通適用。
  - PMW3610デバイス配列へ終端要素を設け、0台・1台・複数台構成へ対応。
  - 慣性減衰率を0.01%単位で指定できる高精度プロパティを追加。
- **影響範囲**:
  - PMW3610ドライバ内部と新しい横方向Behavior。
  - BLE設定、CPI、ZMKの通常キー入力には変更なし。

### 実施内容 (v17: 低速マイクロモーション安定化)

- **目的**: 低速カーソル操作でPMW3610の±1カウント入力が交互に現れ、カーソルが細かくぶれる問題を軽減。
- **変更内容**:
  - 軸ごとに低速入力の方向履歴と保留値を保持。
  - 同方向の入力は確認後に距離を失わず出力。
  - 単発の逆方向入力は保留し、元の方向へ戻った場合はノイズとして相殺。
  - 逆方向入力が継続した場合は意図した方向転換として出力。
  - 設定されたスクロールレイヤーでは安定化処理を自動的にバイパス。
- **影響範囲**:
  - `low-speed-stabilizer`を指定したPMW3610の通常カーソルのみ。
  - 慣性スクロール、通常スクロール、BLE、CPIには変更なし。

### 実施内容 (v18: 稀なカーソル暴走経路の遮断)

- **目的**: input queue輻輳時に未完のX/Yフレームが残り、後続の相対移動へ合算されることで発生し得るカーソルジャンプを防止。
- **変更内容**:
  - PMW3610のmotion、init、慣性、activity処理を専用workqueueへ移動。
  - X送信後にYとneutral syncが失敗した状態を記録し、新しいXを送る前に未完フレームを閉じる処理を追加。
  - 未送信移動量を`max-motion-delta`以内に制限し、50msを超えた場合は破棄。
  - input送信を既定でノンブロッキング化し、1ms間隔の短時間リトライへ変更。
  - MOTなしのactive IRQを1ms間隔で再確認し、20ms継続した場合だけ再初期化。
  - activityによるperformance変更も専用workqueueへ転送し、センサー状態変更を直列化。
- **確認**:
  - AroundFortyAAA左右のZephyr SDK 0.16.5ビルドに成功。
- **影響範囲**:
  - PMW3610ドライバ内部。慣性スクロール、方向トグル、CPI、レイヤー設定は変更なし。
  - 専用workqueueのためRAMを約1.8KB追加使用。BLE接続数とsplit設定は変更なし。
